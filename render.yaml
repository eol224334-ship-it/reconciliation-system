# Render Blueprint Configuration - Static Site
# https://render.com/docs/blueprint-spec
services:
  - type: web
    name: reconciliation-system
    runtime: static
    staticPublishPath: deploy
    pullRequestPreviewsEnabled: false
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.9"
